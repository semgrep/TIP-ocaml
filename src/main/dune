(executables
 (names Main)
 (libraries
    tip_core
    tip_parsing
 )
 (preprocess (pps profiling.ppx ppx_deriving.show))
 ; for ocamldebug
 (modes native byte)
)

(install
 (package tip-ocaml)
 (section bin)
 (files (Main.exe as otip))
)
